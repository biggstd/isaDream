{
  "$schema": "http://json-schema.org/schema#",
  "title": "NMR Experiment Schema",
  "description": "JSON-schema describing an NMR Experiment.",
  "type": "object",
  "properties": {
    "@id": { "type": "string", "format": "uri" },
    "filename" : { "type" : "string"},
    "title": { "type": "string" },
    "description": { "type": "string" },
    "submissionDate" : { "type" : "string", "format" : "date-time" },
    "publicReleaseDate" : { "type" : "string", "format" : "date-time" },
    "publications" : {
     "type" : "array",
     "items" : { "$ref": "publication_schema.json#" }
    },
    "people" : {
      "type" : "array",
      "items" : { "$ref": "person_schema.json#" }
    },
    "qualifiers" : {
      "type": "array",
      "description": "The set of qualitative factors in this experiment.",
      "items": { "type": "string" }
    },
    "quantifiers": {
      "type": "array",
      "description": "The set of unit factors in this experiment.",
      "items": { "type": "string" }
    },
    "qualitative_factors": {
      "type": "array",
      "description": "A set of qualifiers that are shared by every data point associated in this node.",
      "items": { "$ref": "qualitative_factor.json#" }
    },
    "quantitative_factors": {
      "type": "array",
      "description": "A set of quantifiers that are shared by every data point associated in this node.",
      "items": { "$ref": "quantitative_factor.json#"}
    },
    "process_sequence": {
      "type": "array",
      "description": "An ordered list of the processes that are shared by every point associated in this node.",
      "items": { "$ref": "treatment.json#" }
    },
    "samples": {
      "type": "array",
      "description": "The sample(s) used this experiment node.",
      "items": { "$ref": "sample.json#" }
    }
  },
  "additionalProperties": false
}
